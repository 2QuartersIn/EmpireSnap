{
  "name": "empiresnap-desktop",
  "productName": "EmpireSnap",
  "version": "1.9.0",
  "description": "Capture a TradingView indicator's entire settings dialog into one shareable image \u2014 standalone desktop app.",
  "author": "2QuartersIn",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder --win nsis portable",
    "dist:installer": "electron-builder --win nsis",
    "publish": "electron-builder --win nsis --publish always",
    "dist:mac": "electron-builder --mac dmg zip"
  },
  "devDependencies": {
    "electron": "^31.7.0",
    "electron-builder": "^24.13.3"
  },
  "dependencies": {
    "electron-updater": "^6.3.9"
  },
  "build": {
    "appId": "co.2quartersin.empiresnap",
    "productName": "EmpireSnap",
    "files": [
      "main.js",
      "preload.js",
      "ui/**/*",
      "inject/**/*",
      "build/icon.*"
    ],
    "directories": {
      "output": "dist",
      "buildResources": "build"
    },
    "win": {
      "target": [
        "nsis",
        "portable"
      ],
      "icon": "build/icon.ico",
      "artifactName": "EmpireSnap-${version}-${target}.${ext}"
    },
    "portable": {
      "artifactName": "EmpireSnap-${version}-portable.exe"
    },
    "nsis": {
      "oneClick": true,
      "perMachine": false,
      "allowElevation": false,
      "runAfterFinish": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "EmpireSnap",
      "deleteAppDataOnUninstall": false,
      "artifactName": "EmpireSnap-${version}-setup.exe"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "2QuartersIn",
        "repo": "EmpireSnap"
      }
    ],
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": [
            "arm64",
            "x64"
          ]
        },
        {
          "target": "zip",
          "arch": [
            "arm64",
            "x64"
          ]
        }
      ],
      "icon": "build/icon.icns",
      "category": "public.app-category.finance",
      "artifactName": "EmpireSnap-${version}-mac-${arch}.${ext}",
      "identity": null,
      "hardenedRuntime": false,
      "gatekeeperAssess": false
    },
    "dmg": {
      "artifactName": "EmpireSnap-${version}-mac-${arch}.dmg"
    }
  }
}
